<UserControl x:Class="VMS.DV.PD.Scripting.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VMS.DV.PD.Scripting.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DockPanel>
            <Button Content="Print" DockPanel.Dock="Top"/>

            <ListView DockPanel.Dock="Left" ItemsSource="{Binding Fields}"
                      SelectedItem="{Binding SelectedField}" DisplayMemberPath="FieldId"/>

            <Grid DockPanel.Dock="Bottom" DataContext="{Binding SelectedField}" Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>

                <!-- Corner Header -->
                <Border Grid.Row="0" Grid.Column="0" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="" FontWeight="Bold" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Column Headers (Distance) -->
                <Border Grid.Row="0" Grid.Column="1" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="1mm" FontWeight="Bold" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Row="0" Grid.Column="2" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="2mm" FontWeight="Bold" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Row="0" Grid.Column="3" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="3mm" FontWeight="Bold" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Row Headers (Dose Difference) -->
                <Border Grid.Row="1" Grid.Column="0" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="1%" FontWeight="Bold" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Row="2" Grid.Column="0" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="2%" FontWeight="Bold" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Row="3" Grid.Column="0" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="3%" FontWeight="Bold" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Data Cells (1% row) -->
                <Border Grid.Row="1" Grid.Column="1" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="{Binding GammaPassRate_1mm_1pct, StringFormat={}{0:F2}%}" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Row="1" Grid.Column="2" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="{Binding GammaPassRate_2mm_1pct, StringFormat={}{0:F2}%}" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Row="1" Grid.Column="3" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="{Binding GammaPassRate_3mm_1pct, StringFormat={}{0:F2}%}" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Data Cells (2% row) -->
                <Border Grid.Row="2" Grid.Column="1" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="{Binding GammaPassRate_1mm_2pct, StringFormat={}{0:F2}%}" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Row="2" Grid.Column="2" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="{Binding GammaPassRate_2mm_2pct, StringFormat={}{0:F2}%}" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Row="2" Grid.Column="3" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="{Binding GammaPassRate_3mm_2pct, StringFormat={}{0:F2}%}" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <!-- Data Cells (3% row) -->
                <Border Grid.Row="3" Grid.Column="1" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="{Binding GammaPassRate_1mm_3pct, StringFormat={}{0:F2}%}" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Row="3" Grid.Column="2" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="{Binding GammaPassRate_2mm_3pct, StringFormat={}{0:F2}%}" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <Border Grid.Row="3" Grid.Column="3" BorderBrush="Black" BorderThickness="1">
                    <TextBlock Text="{Binding GammaPassRate_3mm_3pct, StringFormat={}{0:F2}%}" Padding="5" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
            </Grid>

            <Grid DockPanel.Dock="Right" DataContext="{Binding SelectedField}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Image Source="{Binding PredictedImage}"/>
                <Image Source="{Binding AcquiredImage}" Grid.Column="1"/>
                <Image Source="{Binding GammaImage}" Grid.Row="1"/>
                <StackPanel Grid.Row="1" Grid.Column="1">
                    <TextBlock Text="Gamma Analysis Criteria" FontWeight="Bold"/>
                    <TextBlock Text="ROI = 5% Threshold"/>
                    <TextBlock Text="Global Gamma"/>
                    <TextBlock Text="Automatic Image Alignment"/>
                    <TextBlock Text="Gamma Image representes 2%/2mm analysis criteria"/>
                </StackPanel>
            </Grid>

        </DockPanel>
    </Grid>
</UserControl>
